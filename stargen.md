StarGen Instructions
====================

In the *StarGenSource* directory, run `make`.

To run the created file, use this command:

    ./stargen -m1.5 -s123456 -g -M -e

Where the number after `-m` is the mass 0.2 to 1.5 and after `-s` is the random seed.
